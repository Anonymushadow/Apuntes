
import 'package:flutter/cupertino.dart';

class PaginaHome extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Bienvenido al inicio", style: TextStyle(fontSize: 30)),
    );
  }
}