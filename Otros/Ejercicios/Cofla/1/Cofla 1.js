

dineroCofla = prompt("cuanto dinero tienes Cofla?");

if (dineroCofla >= 0.6 && dineroCofla < 1) {
	vuelto = dineroCofla - 0.6;
	document.write(" a cofla el dinero le alcanza para un helado de agua y le sobra $" + vuelto);
}
else if (dineroCofla >= 1 && dineroCofla < 1.6) {
	vuelto = dineroCofla - 1;
	document.write(" a cofla el dinero le alcanza para un helado de crema y le sobra $" + vuelto);
}
else if (dineroCofla >= 1.6 && dineroCofla < 1.7) {
	vuelto = dineroCofla - 1.6;
	document.write(" a cofla el dinero le alcanza para un heladix y le sobra $" + vuelto);
}
else if (dineroCofla >= 1.7 && dineroCofla < 1.8) {
	vuelto = dineroCofla - 1.7;
	document.write(" a cofla el dinero le alcanza para un heladovich y le sobra $" + vuelto);
}
else if (dineroCofla >= 1.8 && dineroCofla < 2.9) {
	vuelto = dineroCofla - 1.8;
	document.write(" a cofla el dinero le alcanza para un helardo y le sobra $" + vuelto);
}
else if (dineroCofla >= 2.9) {
	vuelto = dineroCofla - 2.9;
	document.write(" a cofla el dinero le alcanza para un helado con confites o un pote de 1/4kg y le sobra $" + vuelto);
} else {
	document.write(" a cofla no te alcanza para nada y le sobra $" + vuelto);
}


dineroRoberto = prompt("cuanto dinero tienes Roberto?");

if (dineroRoberto >= 0.6 && dineroRoberto < 1) {
	vuelto = dineroRoberto - 0.6;
	document.write(" a roberto el dinero le alcanza para un helado de agua y le sobra $" + vuelto);
}
else if (dineroRoberto >= 1 && dineroRoberto < 1.6) {
	vuelto = dineroRoberto - 1;
	document.write("a roberto el dinero le alcanza para un helado de crema y le sobra $" + vuelto);
}
else if (dineroRoberto >= 1.6 && dineroRoberto < 1.7) {
	vuelto = dineroRoberto - 1.6;
	document.write(" a roberto el dinero le alcanza para un heladix y le sobra $" + vuelto);
}
else if (dineroRoberto >= 1.7 && dineroRoberto < 1.8) {
	vuelto = dineroRoberto - 1.7;
	document.write(" a roberto el dinero le alcanza para un heladovich y le sobra $" + vuelto);
}
else if (dineroRoberto >= 1.8 && dineroRoberto < 2.9) {
	vuelto = dineroRoberto - 1.8;
	document.write(" a roberto el dinero le alcanza para un helardo y le sobra $" + vuelto);
}
else if (dineroRoberto >= 2.9) {
	vuelto = dineroRoberto - 2.9; 
	document.write(" a roberto el dinero le alcanza para un helado con confites o un pote de 1/4kg y le sobra $" + vuelto);
} else {
	document.write(" a roberto no te alcanza para nada y le sobra $" + vuelto);
}

dineroPedro = prompt("cuanto dinero tienes Pedro?");

if (dineroPedro >= 0.6 && dineroPedro < 1) {
	vuelto = dineroPedro - 0.6; 
	document.write(" a pedro el dinero le alcanza para un helado de agua y le sobra $" + vuelto);
}
else if (dineroPedro >= 1 && dineroPedro < 1.6) {
	vuelto = dineroPedro - 1;
	document.write(" a pedro el dinero le alcanza para un helado de crema y le sobra $" + vuelto);
}
else if (dineroPedro >= 1.6 && dineroPedro < 1.7) {
	vuelto = dineroPedro - 1.6; 
	document.write(" a pedro el dinero le alcanza para un heladix y le sobra $" + vuelto);
}
else if (dineroPedro >= 1.7 && dineroPedro < 1.8) {
	vuelto = dineroPedro - 1.7;
	document.write(" a pedro el dinero le alcanza para un heladovich y le sobra $" + vuelto);
}
else if (dineroPedro >= 1.8 && dineroPedro < 2.9) {
	vuelto = dineroPedro - 1.8;
	document.write(" a pedro el dinero le alcanza para un helardo y le sobra $" + vuelto);
}
else if (dineroPedro >= 2.9) {
	vuelto = dineroPedro - 2.9;
	document.write(" a pedro el dinero le alcanza para un helado con confites o un pote de 1/4kg y le sobra $" + vuelto);
} else {
	document.write(" a pedro no le alcanza para nada");
}
